export interface api_liste_des_contrats_d_un_tier {
  dossier:number;
 // obligatoire) : numéro dossier
IncludeAll:boolean ;
  //Défaut=True) : Si False: any;
  //liste limitée aux contrats non résiliés: any;
}
