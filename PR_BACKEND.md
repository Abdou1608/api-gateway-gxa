# feat(backend): int̩gration des routes Express via @fastify/express
- Charge `src/routes/fonctionnalite.routes.ts` (Express) dans le gateway Fastify.
- Conserve `/health`, `/readiness`, `/docs`, proxy `/api/brokers/*`.
